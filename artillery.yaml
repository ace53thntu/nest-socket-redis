config:
  target: "http://localhost:3030"
  phases:
    - duration: 60
      arrivalRate: 10
      rampTo: 30
      name: Test hello world

scenarios:
  - name: "Hello world"
    flow:
      - get:
          url: "/"